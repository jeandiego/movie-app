import React from 'react';
import { Container } from './styles';

import { Logo } from '~/svgs';

const Headers = () => {
  return (
    <Container>
      <Logo />
    </Container>
  );
};

export default Headers;
